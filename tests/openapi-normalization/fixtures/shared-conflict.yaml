openapi: 3.0.3
info:
  title: Campo compartilhado conflitante
  version: 1.0.0
paths:
  /api/customers/{id}:
    parameters:
      - name: id
        in: path
        required: true
    get:
      responses: {}
  /api/customers/{id}:
    parameters:
      - name: code
        in: path
        required: true
    patch:
      responses: {}

